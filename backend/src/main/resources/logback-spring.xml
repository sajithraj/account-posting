<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <springProperty scope="context" name="appName" source="spring.application.name" defaultValue="account-posting"/>

    <!-- ══════════════════════════════════════════════════════════════════
         JSON CONSOLE — structured Logstash format (all profiles)
         All MDC fields are emitted as first-class JSON keys so they are
         queryable in any log aggregator (ELK, Splunk, CloudWatch, etc.).
         ══════════════════════════════════════════════════════════════════ -->
    <appender name="JSON_CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <customFields>{"app":"${appName}"}</customFields>
            <includeMdcKeyName>traceId</includeMdcKeyName>
            <includeMdcKeyName>e2eRef</includeMdcKeyName>
            <includeMdcKeyName>requestType</includeMdcKeyName>
            <includeMdcKeyName>postingId</includeMdcKeyName>
            <includeMdcKeyName>operation</includeMdcKeyName>
            <fieldNames>
                <timestamp>timestamp</timestamp>
                <message>message</message>
                <logger>logger</logger>
                <thread>thread</thread>
                <level>level</level>
            </fieldNames>
            <throwableConverter class="net.logstash.logback.stacktrace.ShortenedThrowableConverter">
                <maxDepthPerCause>20</maxDepthPerCause>
                <shortenedClassNameLength>36</shortenedClassNameLength>
                <rootCauseFirst>true</rootCauseFirst>
            </throwableConverter>
        </encoder>
    </appender>

    <logger name="com.accountposting" level="DEBUG"/>
    <logger name="org.hibernate.SQL" level="INFO"/>

    <root level="INFO">
        <appender-ref ref="JSON_CONSOLE"/>
    </root>

</configuration>
